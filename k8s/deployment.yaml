apiVersion: apps/v1
kind: Deployment
metadata:
  name: coffeday-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: coffeeday
  template:
    metadata:
      labels:
        app: coffeeday
    spec:
      containers:
      - name: coffeeday
        image: dadda5/coffday:20
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
